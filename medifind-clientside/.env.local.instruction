# Google Maps API Setup Instructions

# IMPORTANT: Your current Google Maps API key is not properly configured
# The error messages indicate two issues:
# 1. "ApiNotActivatedMapError" - The Maps JavaScript API is not enabled
# 2. "BillingNotEnabledMapError" - Billing is not enabled on your Google Cloud account

# To fix this:
# 1. Go to Google Cloud Console (https://console.cloud.google.com)
# 2. Select your project (or create a new one)
# 3. Enable the following APIs:
#    - Maps JavaScript API
#    - Distance Matrix API
#    - Geocoding API
#    - Places API
# 4. Set up billing for your Google Cloud account
# 5. Create a new API key with appropriate restrictions
# 6. Replace the key below with your new key

# Note: Keep the API key in .env.local and make sure it stays in .gitignore

NEXT_PUBLIC_BACKEND_URL=http://localhost:5001
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=YOUR_NEW_API_KEY_HERE
